{
  "name": "veri-byte-system",
  "version": "2.0.0",
  "private": true,
  "description": "AI-Based Fake Identity & Document Screening System",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev"
  }
}
